# Public, browser-safe Supabase configuration (placeholders only — no real credentials)
# Filled per environment (DEV / STAGING / PRODUCTION). Never put server-only secrets here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
